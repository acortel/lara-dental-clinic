---
const { class: extraClass, href, text } = Astro.props
---

<a href={href} class={`button ${extraClass}`}>
    {text}
</a>

<style>
    .button {
        display: inline-block;
        padding: 0.625rem 1.5rem;
        font-family: "Orelega One", serif;
        font-weight: 500;
        font-size: 1.25rem;
        border-radius: 10px;
        transition: background-color 0.3s ease;
        text-align: center;
        background-color: #F58EBB;
        columns: #fafafa;
        color: #fafafa;
    }

    .button:hover {
        background-color: #D84DA0;
    }

    .hero-cta {
        padding: 1.2rem 2.75rem;
        font-size: 2.25rem;
    }

    @media (max-width: 768px) {
        .hero-cta {
            padding: 0.625rem 1.5rem;
            font-size: 1.25rem;
        }
    }
</style>